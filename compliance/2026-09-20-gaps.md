# COMPLIANCE GAPS — 2026-09-20

**What this file is:** authored analysis laid over a *generated* registry. Every number quoted
here is copied from `local/compliance/REGISTRY.md` (emitted 2026-09-20 by
`local/scripts/constitution-sweep.sh` — re-run it, never edit it). This file is not law and not
a gate: it files findings the way `CONSTITUTION.md` §2 requires (agents propose, the Owner
rules in writing). **Nothing in any project was edited, re-pinned, or repaired.**

---

## 0. What was run (commands + exits)

| command | exit | what it established |
|---|---|---|
| `bash local/scripts/constitution-sweep.sh --self-test` | 0 | the sweep catches a planted lie (missing path, em-dash gate, unresolved referents) and passes a planted control row — known-positive rule, same discipline as `Projects/notrick/tools/citecheck.sh --self-test` |
| `bash local/scripts/constitution-sweep.sh --write` | 0 | emitted `REGISTRY.md` + `registry.tsv`; counts generated at read time (R14) |
| `bash local/scripts/constitution-sweep.sh --check --run-gates` | **1** | 5 findings, listed in §2/§3 below. The exit 1 is the point: the gate does not go green while the constitution names a gate that is not there |
| `bash -n local/scripts/constitution-sweep.sh` | 0 | syntax clean |

The registry now carries its own provenance — `source digest: sha256=efc0ac11… · bytes=8810 ·
mtime=2026-09-20 11:22:44` — and **the law moved while this program ran** (see §2b). That digest
is the revision every number below was computed against; a registry whose digest differs from
the file on disk was graded against a different revision and must be regenerated before quoting.

Live gate observations from that same run (this machine, 2026-09-20):

| §2 row | gate command | rc | last line |
|---|---|---|---|
| NOTRICK | `sha256sum -c <(grep -E "^[0-9a-f]{64}" INTEGRITY.md)` | 0 | `./ZERO-READINESS.md: OK` |
| Colony Kernel | `npm run verify` | 0 | `verify: ALL GATES GREEN` |
| dyno-pony | `node --test tests/*.test.cjs` | 0 | `ℹ duration_ms 84.174462` |
| DSH | `pnpm run doc-sync` | — | tier 2, declared-only (not run; `--heavy` not passed) |

NOTRICK's 44 pins re-verified in this session: **44 OK / 0 FAILED** (`grep -cE '^[0-9a-f]{64}'
INTEGRITY.md` → 44). dyno-pony's README claims "171 tests green" and the oracle printed
`tests 171 / pass 171 / fail 0`.

**Counts (from `REGISTRY.md` §2):** `rows_total=9  paths_total=12` ·
`gate_command=4 (tier1=3 tier2=1 not-runnable=0)` · `gate_none=3  gate_prose=1  gate_external=1` ·
`paths_missing=1` · `referents_ok=30  referents_missing=0  shorthand=1  non_local_tokens=1`.

---

## 1. §2 rows that pass mechanically

7 of 9 rows resolve to a live path with every protected referent present (30 referents checked,
0 unresolved). Two notes, both printed by the tool rather than silently resolved:

- `[L57 dyno-pony]` `collect.sh` does not exist at the repo root; it resolves to
  `scripts/collect.sh` (class `SHORTHAND`). The row is correct in substance, imprecise as text.
- `[L54 Hermes Agent]` `NousResearch/hermes-agent` is an upstream repo name, not a local path
  (class `NOT-A-LOCAL-PATH`) — printed so it is visibly skipped, never silently dropped.

---

## 2. Gaps inside §2 (4 rows)

| # | row (line) | finding | proposal (Owner-ruled, not applied) |
|---|---|---|---|
| G1 | `crew-research-council` (L55) | gate column is `—`: **no gate declared**, and none exists in the tree — 52 tracked files, all `.md` + `.gitignore`; no runner, no `.json`, no `.sh`, no `.py` | either (a) declare a real gate (see §3 — the harness must be restored or rebuilt first for that to mean anything), or (b) record the row explicitly as `NO GATE (by design)` so the sweep can classify it instead of filing it forever |
| G2 | `Anatomy Lab` (L56) | gate is **prose** (`lab protocol (S1..S6 / D1..D8 / G1..G7 / K1..K6 / T1..T5)`), not a command; the sweep prints the gap rather than pretending prose is executable | the material for a real gate already exists on disk: `anatomy-lab/catalog.json` records `sha256` + `bytes` per profile against `souls/**`. A read-only check "recompute sha256 of each `souls/<profile>.md`, compare with `catalog.json`" would test the one-way-sync claim without touching a protected byte. Proposal only — `souls/**` and `catalog.json` stay untouched (§2) |
| G3 | `knowledge-factory` (L58) | gate column is `—`: **no gate declared** | the row already names exactly one protected path. A SHA-256 manifest over `wiki/archive-2026-09-05-harness-divergent-copy/**` (imported verbatim, "do not merge blind") would turn that sentence into a check. Proposal only |
| G4 | runtime row (L59) | gate column is `—`, covering `.dsh/`, `.openclaw/`, `~/.claude`, `~/.agents` | not a project — the row says "executable state only — no doctrine". Proposal: mark the row by design (`NO GATE (runtime, no doctrine)`) so `--check` stops counting it as an undeclared gate |

### G4's claim, measured (REGISTRY.md §5)

| path | `*.md` (depth ≤3) | doctrine-keyword files |
|---|---|---|
| `.dsh/` | 105 | 3 |
| `.openclaw/` | 2 | 0 |
| `~/.claude` | *(absent)* | — |
| `~/.agents` | 17 | 0 |

The three `.dsh/` hits are **not doctrine**: two are *attachments* (uploaded copies of
`NOTRICK_SERIES2_00/01` manuals, under `.dsh/attachments/v1/files/**`) and one is an agent note
(`.dsh/memory/per-agent/2026-09-18-colony-kernel-published-and-session-coordination.md`). So the
claim "executable state only — no doctrine" survives the keyword test as written; what the
measurement adds is the *category* the claim does not name: attachments and per-agent memory.
If the Owner wants that pinned down, the honest wording is "no *law*; inputs and memory allowed".

### 2b. Concurrent writer — the law moved mid-program, and the declarations landed

`CONSTITUTION.md` was edited **during this session**. The establishing evidence is content, not
mtime: my first read of the file ended at line 98 with §2's rows at lines 43–51, and the same
file now has 106 lines with those rows at 51–59 plus a new paragraph at 41–47; its size went
8192 → 8810 bytes between the opening `ls` and the sweep. (`mtime 11:22:44` — the machine clock
appears to have stepped forward during the session, since this tool's later writes carry 21:1x;
timestamps across that step are not comparable, so they are cited, not relied on.)

The change is a new law paragraph at lines 41–47 ("One home per fact"): a project carrying a
`PROTECTED.md` at its root **declares its own zones there and that file is
authoritative** — the §2 table is only the index. It names four projects with local declarations
(`Projects/dyno-pony`, `Projects/knowledge-factory`, `crew-research-council`, `anatomy-lab`) and
four that keep protection in their own doctrine (NOTRICK `INTEGRITY.md`, Colony Kernel
`GOVERNANCE.md` + `AGENTS.md`, DSH `AGENTS.md` + `vendor/README.md`, upstream Hermes). The edit
shifted the §2 rows from lines 43–51 to 51–59; the registry's line citations are from the
**post-edit** file.

In the same window, another session created the declarations and compressed prose:

| file | mtime | what |
|---|---|---|
| `crew-research-council/PROTECTED.md` | 11:22:26 | new; declares `RESEARCH-COUNCIL/**`, `STATUS.md`, and the untracked `tests/** crew/** harness/** repro/**` trees — and says plainly: "There is no build gate in this project" |
| `Projects/knowledge-factory/PROTECTED.md` | 11:21:54 | new; gate section: "**None yet** — there is no plugin, no composition, and no executable surface here" |
| `Projects/dyno-pony/PROTECTED.md` | 11:21:54 | new; gate section carries the real commands (`node --test tests/preflight.test.cjs`, `node --test tests/*.test.cjs`) |
| `anatomy-lab/PROTECTED.md` | 11:2x | new; 11 protected referents, all resolving (registry §6) |
| `crew-research-council/README.md`, `Projects/{dyno-pony,knowledge-factory}/README.md`, `Projects/colony-kernel/{AGENTS.md,README.md}` | 11:14–11:18 | §3 compression-style rewrites (colony: 40 insertions / 45 deletions) |

Consequences for this file, stated plainly:

1. **G1 and G3 change shape.** The gate facts now exist at the project level — crew declares "no
   build gate" and knowledge-factory declares "None yet", both *explicitly*. The §2 amendment is
   therefore no longer "invent a gate"; it is "let §2's gate cell point at the local
   declaration" (the index role the new law assigns it). The mechanical finding stands: §2's own
   cell is still `—`, so `--check` still files it.
2. **G2's proposal is now backed by the project's own file.** `anatomy-lab/PROTECTED.md` exists
   with all 11 of its referents resolving; the `catalog.json` sha256 idea can be proposed as an
   addition to that file rather than as an invention from outside.
3. **The sweep had to follow the law, not the other way round.** It now reads the two lists out
   of the law's own prose (no hardcoded names), reports `PROTECTED.md` presence per named
   project *and* resolves that file's own referents (registry §6), and fails loud on both
   violation classes: a named local declaration that is missing, and a "two homes" project
   (doctrine-keeper) that grows a `PROTECTED.md`. Today: 4 of 4 declarations present
   (11 + 6 + 8 + 3 = 28 referents resolved, 0 missing) and 0 two-homes violations.
4. **A correction to my own baseline.** My first measurement of dirty-file counts was taken at
   11:14, in the middle of those rewrites; the later rise (dyno-pony 1→2, knowledge-factory 1→2,
   crew 0→2) is the parallel writer's work, provable by mtime, not an effect of this tool —
   which writes only under `local/compliance/`.

---

## 3. Evidence gaps found while sweeping (records that cannot be quoted from disk)

These are not §2 rows. They are claims in the workspace that no verifier can currently quote
from disk — the failure mode `CONSTITUTION.md` §1 opens with.

**E1 — `crew-research-council/GOAL.md` cites an artifact that does not exist.**
`GOAL.md` records `DECISIVE-01 CLOSED (2026-09-12, see experiment/VERDICT.md)` with the numbers
**A 12/16, trio 10/16, system-chosen 8/16**, and later `REPLICATION CLOSED` with
**A 15/20 · B 14/20 · C 10/20**. `experiment/VERDICT.md` is on no disk: not in the repo, not in
`~/.hermes/crew/`, not under `~/crew/` (searched; the only `VERDICT.md` files in the workspace
are `Projects/notrick/mill/VERDICT.md` and `Projects/notrick/hept/VERDICT.md`). Reproduce:
`find /home/fares -maxdepth 5 -name 'VERDICT.md' -not -path '*/.git/*'`.

**E2 — the crew harness exists only as bytecode.**
`crew-research-council` tracks 52 files; every one is `.md` except `.gitignore`. Nine `.pyc`
files survive and are valid CPython-3.12 bytecode — verified by unmarshalling, not by trusting
the filename. Recovered API surface:

| module | recovered top-level names |
|---|---|
| `crew/router` | `complexity_score`, `propose_formation` |
| `crew/roles` | `_check_constraint`, `run_executor`, `_task_sources`, `run_researcher`, `run_reviewer` |
| `crew/abstain` | `is_abstain`, `is_abstain_v2` … `is_abstain_v5` |
| `harness/state` | `Budget`, `RunLedger` |
| `repro/run_repro` | `score_stub`, `judge_transfer` v1–v5, `_transfer_rows`, `check_protocol`, `main` |
| `tests/test_crew` | 11 classes incl. `TestGraderAudit`, `TestAdversarialProbes`, `TestReproFixture` |

So v2's Phase-1 slice (router + formation choice), the COORD-01 constraint fix
(`_check_constraint`), five generations of the abstention rule and of the judge are *recoverable
in shape* and *gone in source*. `v2-ARCHITECTURE.md` is unbuilt; only
`~/crew/experiment/v2-test-2/{api,cli,convert,cache,test_all}.py` survived as output of one run.

**E3 — a roster count that no store backs.**
`crew-research-council/PROJECT.md:17` — "Entities: 81 profiles under `~/.hermes/profiles/`
(ls counted)". Disk today: `~/.hermes/profiles`=16 · `~/.dsh/profiles`=4 ·
`~/.config/dsh/profiles`=1 · `~/Archive/dsh_backup-2026-09-19/profiles`=4. A named plausible
cause exists in the record, not in this file's numbers: the 2026-09-19 repo-boundary migration
(`local/migrations/2026-09-19-crew-research-council-migration.md`, `.gitignore` untracking
`.hermes/` working copies, home strays quarantined to `~/Archive/20260919-*`). The claim is
*stale-after-migration*, which is a different thing from false — but as of today it is
unverifiable, and the sweep cannot grade it.

**E4 — empty and orphaned crew dirs.** `~/.hermes/crew/tests/unit/` and `.../e2e/` are empty
directories; `~/.hermes/crew/experiment/tasks/` holds 2 task dirs (`05-sql-hard`,
`11-debug-repo`). `~/.hermes/crew/src/crew/**` is a *different* project (memory layers:
`mediated_memory.pyc`, `mem0_layer/`, `unified_memory/`) and its sources are gone too.

Each of E1–E4 is a one-line claim plus the command that reproduces it — the shape a repair
proposal can be filed in. **No proposal is filed by this round**; the goal was to make the gaps
quotable first.

---

## 4. Coverage — trees on disk vs §2

`candidates=28 covered=7 containers=1 uncovered=20` (declared heuristic flag:
`looks-like-a-project` = git repo or doc/config file at depth ≤2; 12 of the 20 carry it).

- **covered (7):** the five `Projects/*` rows plus `crew-research-council`, `anatomy-lab`,
  `hermes-agent` (under `~/.hermes`) — i.e. exactly the trees §2 names.
- **container (1):** `Projects` — excluded from the gap count because its children are covered.
- **uncovered (20), with the heuristic flag:** `Archive`, `local`, `crew`, `2026-research-council`,
  `Applications`, `cleanup`, `Downloads`, `Research`, `research agents`, `sidon-exp`, `VPNs`
  [looks-like-a-project]; `Camera`, `Cline`, `hermes`, `openresearch`, `Screenshots`, `skills`,
  `snap`, `truth`, `Webcam` [—].

Three of those are worth an Owner ruling rather than a shrug, because the workspace's own
records lean on them: **`~/local`** (home tooling, migration records — and now this registry),
**`~/Archive`** (the quarantine convention NOTRICK's close commit names: `~/Archive/20260919-*`),
and **`~/crew`** (the surviving v2 experiment outputs). The proposal shape is the same for each:
either a §2 row (`path | protected | gate`) or an explicit `OUT-OF-SCOPE` line. The sweep will
not invent either.

---

## 5. What would make `--check` exit 0

1. Owner rulings on G1–G4 — four §2 *text* amendments, now cheap because the substance exists:
   G1/G3 point the index at the local declarations created at 11:21–11:22 (§2b); G2 is backed by
   `anatomy-lab/PROTECTED.md`; G4 marks the runtime row as no-gate-by-design.
2. `~/.claude` resolved: either the row drops it, or the path exists (today: `NO`).
3. Nothing else. As of this run: 9 §2 rows / 12 path cells parsed · 30 of 30 §2 protected
   referents resolve · 28 of 28 local-declaration referents resolve (§6) · 3 of 3 tier-1 gates
   executed and returned 0 · 0 two-homes violations.

Until (1) and (2) land, `--check` exits 1 **by design**, and that is the honest state: a
constitution whose §2 cannot be fully executed is not yet complete by its own standing
consequence.

---

## 6. How to re-run (the whole program, three commands)

```
bash ~/local/scripts/constitution-sweep.sh --self-test    # prove the checker still catches a lie
bash ~/local/scripts/constitution-sweep.sh --write        # regenerate REGISTRY.md + registry.tsv
bash ~/local/scripts/constitution-sweep.sh --check --run-gates   # the gate: exit 0 when §2 is executable
```

`--run-gates` adds `--heavy` to include tier-2 monorepo gates (`pnpm run …`), and
`GATE_TIMEOUT=<seconds>` bounds each one (default 300). The sweep reads only: no project file is
written by it, and the two files it does write are its own, under `local/compliance/`.

**Before quoting any number:** compare the registry's line 4 (`source digest: sha256=…`) with
`sha256sum ~/CONSTITUTION.md`. In this workspace the law is a moving input — it moved once
already during this program (§2b) — and a registry whose digest does not match was graded against
a different revision.

---

## UPDATE 2026-09-20 (later the same day) — E1 and E2 changed status

The crew-evidence program (mission `CREW-EVIDENCE`) recovered what §3 of this file marked as
missing. Pointers that supersede, never rewrites:

- **E1 (missing verdict)** → a dated **re-run** now exists at `~/.hermes/crew/experiment/VERDICT.md`
  (five arms, 20 tasks, seed 7, offline, deterministic, independently reproduced 5/5). The
  *original* 16-task verdict is still absent, and its recorded per-arm numbers (A 15/20 · B 14/20 ·
  C 10/20) remain **UNREPRODUCED** — the surviving fixture has 20 tasks.
- **E2 (bytecode-only harness)** → the source was found alive in a volatile `/tmp` working copy and
  rescued to `~/.hermes/crew/experiment/rescued/2026-09-20-verify-consol/` (69 files, verified
  manifest). Recursive fingerprinting: **6 of 9 modules identical** to the repo bytecode; the other
  three are a later revision whose only delta is the v5 judge — transcribed from the disassembly and
  verified by that revision's own compiled tests (`ok=True run=47`; source suite still 46/46).
- **E3** (the 81-profile claim) and **E4** (empty crew dirs) are unchanged by that program.
- The four §2 amendments (G1–G4) and `~/.claude` are also unchanged: `--check` still exits 1.

Evidence: `~/crew-research-council/missions/CREW-EVIDENCE/2026-09-20-EVIDENCE-RECOVERY.md` ·
`~/crew-research-council/missions/CREW-EVIDENCE/BOARD.md` ·
`~/.hermes/crew/experiment/runs/2026-09-20-replication/VERIFIER-REPORT.md`.



