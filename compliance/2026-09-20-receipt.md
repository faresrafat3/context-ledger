# RECEIPT — constitution compliance sweep (rounds G1–G4)

**Date:** 2026-09-20 · **Owner's goal:** "مسح امتثال الدستور على كل المشاريع — كل مشروع يعلن gate
command + protected zones، والـregistry يتولّد من الديسك وقت القراءة"
· **Status:** goal met for the mechanical half; the Owner-ruled half is filed, not applied.

---

## 1. Artifacts written (nothing else was touched)

| path | kind | note |
|---|---|---|
| `local/scripts/constitution-sweep.sh` | tool (new) | reads `CONSTITUTION.md` §2 from disk, probes every row, emits counts at read time; coreutils-only (`ls cat head tail wc grep sed sha256sum git find`, no awk/jq/python) |
| `local/compliance/REGISTRY.md` | **generated** | the registry; re-run, never edit |
| `local/compliance/registry.tsv` | **generated** | machine-readable twin of the same run |
| `local/compliance/2026-09-20-gaps.md` | authored | findings G1–G4 + evidence gaps E1–E4, each with the command that reproduces it |
| `local/compliance/2026-09-20-receipt.md` | authored | this file |

**Zero writes into any project.** No doctrine edit, no re-pin, no `CONSTITUTION.md` edit, no
change inside a protected zone (`mill/**`, `souls/**`, `RESEARCH-COUNCIL/**`, `INTEGRITY.md`'s 44
pins, `vendor/**`, `snapshots/**`). The only files created are the five above.

## 2. Commands + exits (this session)

| command | exit | effect |
|---|---|---|
| `bash -n local/scripts/constitution-sweep.sh` | 0 | syntax clean |
| `bash local/scripts/constitution-sweep.sh --self-test` | 0 | `SELF-TEST: PASS` — planted lie caught, control row untouched |
| `bash local/scripts/constitution-sweep.sh --write` | 0 | emitted the two generated files |
| `bash local/scripts/constitution-sweep.sh --check --run-gates` | **1** | `failures=5` — 4 §2 gaps + 1 missing path; the intended state until the Owner rules |
| `npm run verify` (colony-kernel, via `--run-gates`) | 0 | `verify: ALL GATES GREEN` — 66 tests, demo byte-identical (5613 bytes) |
| `node --test tests/*.test.cjs` (dyno-pony, via `--run-gates`) | 0 | `tests 171 / pass 171 / fail 0` |
| `sha256sum -c <(grep -E '^[0-9a-f]{64}' INTEGRITY.md)` (NOTRICK, via `--run-gates`) | 0 | 44 pins OK / 0 FAILED |
| `sha256sum` pin count (NOTRICK, separate) | 0 | header declares 44; 44 resolve |

## 3. Concurrent writer observed — not this tool's writes

While this program ran, **another session was editing the same workspace**. The establishing
evidence is content, not mtime: `crew-research-council` returned `git status --porcelain | wc -l`
= **0** early in the session and **2** later, the new entry being an untracked `PROTECTED.md`
(dyno-pony 1→2 and knowledge-factory 1→2 the same way); `anatomy-lab` had no `PROTECTED.md` in
the opening listing; and `CONSTITUTION.md` grew 8192 → 8810 bytes (98 → 106 lines) with §2's rows
moving from lines 43–51 to 51–59, adding a law paragraph at 41–47 that makes a project-root
`PROTECTED.md` the authoritative zone declaration and demotes §2 to index. Mtimes for the
parallel writes cluster at 11:21–11:22 — cited, not relied on: this machine's clock appears to
have stepped forward during the session (these artifacts carry 21:1x), so timestamps across that
step are not comparable, and nothing here rests on them.

Consequences, recorded rather than smoothed over:

- the registry's line citations (51–59) and the digest `efc0ac11…` are from the **post-edit**
  revision; a reader with the earlier file in hand will see a mismatch — that is why the digest
  line was added to the registry header;
- the sweep was extended the same round to *follow* the new law: it reads the two name lists out
  of the law's own prose, checks `PROTECTED.md` presence, resolves those files' own referents,
  and fails loud on a missing named declaration or a "two homes" project;
- my opening dirty-file counts were taken at 11:14, mid-rewrite; the later rise
  (dyno-pony 1→2, knowledge-factory 1→2, crew-research-council 0→2) is that session's work,
  provable by mtime. This tool's only write path is `local/compliance/`.

## 4. Failure record — seven defects in this tool, all caught by its own tests

Kept on the record because the tool's whole claim is that it can be believed:

1. read §1's constant table as if it were §2 (`^\| \*\*` matched both) → scoped to `## 2.`–`---`,
   with a fixture fallback so `--self-test` still exercises the same code path.
2. `--self-test` broke when the §2 scoping landed (fixture has no `## 2.`) → caught immediately,
   fixed by the fixture fallback above. **The known-positive rule earned its keep here.**
3. `not-runnable` counted 17 because `grep -c '\t0\t'` matched numeric fields anywhere →
   replaced with per-row, field-indexed counting (`count_row_kind`).
4. `collect.sh` (dyno-pony) flagged as a lost file; it exists as `scripts/collect.sh` → new class
   `SHORTHAND` (bare basename resolved deeper in the tree), separate from `MISSING`.
5. `set -euo pipefail` + `grep` with no matches aborted the runtime scan mid-report; `--write`
   therefore wrote only one file and `--check` exited 1 **from a crash, not from its own
   findings** → `|| true` on every pipeline in the scan. **This one nearly produced a false
   claim; it is why the receipt records the exit codes separately from the findings.**
6. `hits` counted joined lines instead of files, and the doctrine footnote printed for absent
   paths → count files before joining; skip non-numeric counts.
7. `scripts/collect.sh --apply` (dyno-pony's `PROTECTED.md`) read as a missing file because the
   token carried its arguments → a token that does not resolve is now retried by its first word;
   arguments are not paths.

## 5. What was NOT done (honest ceiling)

- **No §2 amendment was made.** G1–G4 and the `~/.claude` path are proposals; `CONSTITUTION.md`
  was read, never written. Applying them is an Owner act (R8). Note that the *substance* of the
  fix already landed from the parallel session (§3): four projects now carry their own
  `PROTECTED.md`, so the amendment is an index link, not an invention.
- **The new law was followed, not adjudicated.** The sweep now reads the "One home per fact"
  paragraph and enforces its two mechanical consequences (named declaration must exist; a
  doctrine-keeper must not grow a second home). Whether the four named projects are the *right*
  four is an Owner question this tool does not touch.
- **No DSH gate was executed.** `pnpm run doc-sync` is tier 2 (monorepo, can build a full
  `ts.Program` per gate); it is reported as declared-only, not as green.
- **No repair of E1–E4.** The missing `experiment/VERDICT.md`, the bytecode-only harness, the
  unreproducible 81-profile count and the empty crew test dirs are filed as evidence gaps. Fixing
  them is a different program (recover → rebuild → re-run), and the crew's own `GOAL.md` law
  freezes births and team-layer changes while the captain is away.
- **Coverage is 20 uncovered trees / 28 candidates, unresolved by design.** The tool refuses to
  invent a row for a tree the Owner has not ruled on; the three that carry real weight
  (`~/local`, `~/Archive`, `~/crew`) are named in `2026-09-20-gaps.md` §4.
- **The sweep does not grade project-internal truth.** It checks that a gate *exists, is
  runnable, and (if run) returns its own success line*. It cannot tell whether a green gate
  proves what the project claims it proves — that stays the project's own verifier's job.

## 6. Resume in one command

```
bash ~/local/scripts/constitution-sweep.sh --check --run-gates
```

Two things change the exit code to 0: the four §2 amendments and `~/.claude`. Until then the
program's standing output is: **9 rows parsed · 12 paths probed · 30 of 30 protected referents
resolve · 3 of 3 tier-1 gates green · 5 findings filed and unfixed.**
